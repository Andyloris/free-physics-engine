#include <stdheaders.h>
#include <vectors.h>
#include <particle.h>
#include <object.h>
#include <scene.h>

int main()
{
	/*scene_t scene = scene_alloc(1);

	object_t obj;

	memset(&obj, 0, sizeof(object_t));

	obj.center.mass = 2;
	obj.radius = 3;

	scene_set_objects(&scene, &obj);

	scene_update(&scene);
	/*particle_t part;
	memset(&part, 0, sizeof(part));

	part.mass = 1;

	vector3d_t force;

	force.x = 1;
	force.y = 0;
	force.z = 0;

	particle_apply_force(&force, &part);
	while(1)
	{
		particle_update(&part);
		printf("x: %lf, y: %lf, z: %lf\n", part.position.x, part.position.y, part.position.z);
	}*//*
	
	scene_free(&scene);*/

    return 0;
}
